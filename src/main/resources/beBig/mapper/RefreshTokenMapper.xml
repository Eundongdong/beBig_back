<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="beBig.mapper.RefreshTokenMapper">

    <!-- Refresh Token 저장 -->
    <insert id="saveRefreshToken">
        INSERT INTO refresh_token (user_id, token, expiry_date)
        VALUES (#{userId}, #{token}, #{expiryDate})
    </insert>

    <!-- Refresh Token 조회 -->
    <select id="findByToken" resultType="beBig.vo.RefreshTokenVo">
        SELECT id, user_id, token, expiry_date
        FROM refresh_token
        WHERE token = #{token}
    </select>

    <!-- Refresh Token 삭제 -->
    <delete id="deleteByToken">
        DELETE FROM refresh_token
        WHERE token = #{token}
    </delete>

    <!-- 특정 유저의 Refresh Token 삭제 -->
    <delete id="deleteByUserId">
        DELETE FROM refresh_token
        WHERE user_id = #{userId}
    </delete>

</mapper>
